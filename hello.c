// #include <cs50.h>
#include <stdio.h>
// header files - files written in C e.g. stdio.h(Standard Input/Output)
// gives you access to more functions that the ones you would automatically get
// stdio.h gives you the ability to get input and output from the user by providing with printf
int main(void) {
    printf("hello, world!");
}

// string answer = get_string("What is your name");
// get_string is only specific to CS50 IDE
// printf("hello, %s\n", answer);
// \n(escape/new line) - move the cursor to a new line
// to compile the code to machine code
// make hello
// to run the code
// ./hello

// help50 - cs50 debugger? that allows you to troubleshoot problems
// help50 make hello -this would show rhetoric guide-intended question
// style50 - helps improve style on CS50 IDE
// check50 - to check the correctness of the code
// mv hello.c lecture/
// mv hello.c .. - move it one level up i.e. to parent(home) directory
// rmdir - to remove a directory
// .(single dot) - current directory
// ./hello.c - run program called hello.c in current directory
// bool, char, double, float, int, long, string
// cp - copy
// int in C - 32bits
// long - 64bits
// printf format codes
// %c - to print a single character
// %f(float), %i(integer), %li(long integer), %s(end)